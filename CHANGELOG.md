# CHANGELOG

すべての重要な変更はこのファイルに記録されます。

---

## [2026-08-27] v2.4.0 - デスクトップショートカット作成、未アノテーション画像の空ラベル自動生成、UI整理

### 追加 (Added)
- **デスクトップ起動ショートカット作成機能 (設定画面)**: 設定画面下部に「ショートカット作成」ボタンを追加。OS（Windows / Raspberry Pi / Linux）を自動判別し、デスクトップ上にアプリ起動用ファイル「YOLOマネージャー.bat」または「YOLOマネージャー.sh」（ターミナル自動立ち上げ対応・終了時待機機能付き）をワンクリックで自動作成する機能を追加。
- **Raspberry Pi 環境における「学習実行」ボタンの完全無効化**: Raspberry Pi 環境（`/proc/device-tree/model` 等の自動判定）を検出した際、「5. 学習実行」ボタンを自動的にグレーアウト・クリック不可（`5. 学習実行 (RPI非対応)`）に設定し、誤操作や過負荷学習を防止するセーフティ機能を追加。
- **未アノテーション画像に対する空ラベルファイル (.txt) 自動生成**: ステップ5「学習実行」開始時に、`train` / `val` フォルダ内のラベル未設定画像（対応する `.txt` がない画像）に対して自動で空の `.txt` ファイルを作成する機能を追加（背景画像・ネガティブサンプルとして YOLO に正常認識させる）。

### 変更・削除 (Changed / Removed)
- **左パネル進捗バーの削除**: 操作ステップ下のプログレスバーを削除し、UIをシンプルに整理。

---

## [2026-08-19] v2.3.0 - データセット準備 (画像コピー) の非同期化とリアルタイム進捗表示

### 追加 (Added)
- **CUDA Out of Memory (VRAM不足) 自動リカバリ・VRAM解放機構**: 学習開始直前に `torch.cuda.empty_cache()` および `gc.collect()` で VRAM キャッシュを完全解放。万が一 Auto-Batch や高解像度 (`imgsz=800` 等) 学習時に GPU VRAM が不足して `CUDA error: out of memory` が発生した場合、スクリプト内で自動的に VRAM を解放し、安全なバッチサイズ (`batch=4` / `batch=2`) に自動リトライ・フォールバックして学習を自動リカバリする機能を追加。
- **ログウィンドウへのリアルタイム進捗表示**: コピー処理中、メイン画面のログウィンドウに枚数・パーセント・ファイル名（例: `[INFO] コピー中: 150 / 500 枚 (30%) - image_0150.jpg`）を動的にログ出力する機能を追加。

- **設定画面への「並列数 (workers)」設定項目の追加**: 設定画面の「ハイパーパラメータ」グループに「並列数 (workers)」の入力項目（0〜32）を追加。GPU・CPU環境に合わせてユーザーが任意のデータローダー並列数を直接指定・保存できるよう改善。

- **「動作停止」ボタンの追加 (ログクリアボタン左側)**: 画面右下のログエリア下部に「動作停止」ボタンを追加。学習・推論・画像コピー等の処理実行中にクリックすることで、実行中のプロセス・タスクを即座に安全中断・停止可能。処理実行中のみ有効化され、待機中は無効化。

- **操作ステップ下の進捗バー (QProgressBar) の全自動連動化**: 左パネルの操作ステップ直下に配置されている進捗バーを活性化。画像コピー時は枚数・パーセント（`30% (150/500 枚)`）、学習・推論・自動アノテーション時はログテキストから進捗率（`35%`, `Epoch 15/50` 等）を自動抽出してリアルタイム表示・全自動連動されるよう改善。

- **ステップ完了時の進捗バー 100% 表示固定**: 全てのステップ・ワーカー処理が完了した際、操作ステップ直下の進捗バーが必ず `100%` に到達し、完了状態を分かりやすく明示するよう改善。
- **キャリッジリターン (\\r) 対応バイナリストリームリーダーによる完全リアルタイムログ出力**:
  - `tqdm`（エポック内のバッチ計算進行バー）が出力する `\r` を `\n` を待たずに即時検出・配信するバイトストリームリーダー（`_read_process_output_realtime`）を実装。エポック処理中にログが停止することなく、1バッチごとに滑らかに進捗が更新されるよう改善。
  - `PYTHONUNBUFFERED=1` および `python -u` オプションと連携し、エポック進捗（`1/200`, `2/200` 等）から正確に進捗率を算出して進捗バーに連動。
- **一部コンソールログの日本語化と無反応状態の解消**: キャッシュ生成ログ（`train/val: New cache created` ➔ `[INFO] 学習用/検証用キャッシュを作成しました`）、AutoBatch試行開始ログ（`AutoBatch: Computing...` ➔ `[INFO] 最適なバッチサイズを自動計算中...`）、データセットスキャンログ（`train/val: Scanning` ➔ `[INFO] 学習用/検証用データセットをスキャン中... 60%`）、ラベル分布画像保存ログ（`Plotting labels to` ➔ `[INFO] ラベル分布画像を保存しました`）を分かりやすい日本語メッセージに変換。連続スキャンログは同一1行で上書き更新されるよう改修し、キャッシュ作成後のバッチサイズ計算中も明確なステータスが表示されるよう修正。
- **モデル構造・AutoBatch等の不要内部ログの自動除外**: モデルレイヤー一覧テーブル（`from n params module`）、パラメータサマリー、転送・フリーズ情報、AMPチェック、AutoBatch計算・結果テーブルなどの各種ノイズログをフィルタリング除外。

---

## [2026-08-12] v2.2.0 - P2ヘッダーモデル対応・NCNNフォルダ名末尾.ncnn付与・設定UI自動化

### 追加 (Added)
- **P2ヘッダー付きモデルへの対応**: 小物体検出用 P2 Detect Head を持つベースモデル (`n-p2`, `s-p2`, `m-p2`, `l-p2`, `x-p2`) に対応。設定画面のベースモデルドロップダウンで選択でき、YOLO11 / YOLOv8 の P2 モデル（例: `yolo11n-p2.pt` / `yolov8n-p2.pt`）の学習・推論が実行可能。
- **NCNN モデル変換フォルダ末尾 `.ncnn` リネーム**: NCNN モデルエクスポート完了時、自動生成されたフォルダの末尾に `.ncnn` を自動付与し `best_ncnn_model.ncnn` として保存・出力する機能を追加。
- **折りたたみ式オーギュメント設定グループ (CollapsibleGroupBox)**: 設定画面の「オーギュメント設定」を初期状態で閉じた (折りたたんだ) コンテナとして配置。ヘッダーをクリックすることで確実にスライド展開・折りたたみが可能。

### 変更 (Changed)
- **設定項目からのバッチサイズ・並列数入力欄の削除と自動化**: ユーザー設定項目から「バッチサイズ」および「並列数」を削除。バッチサイズは常に `-1` (Auto-Batch) に固定、並列数は `imgsz` の値に応じて動的算出（`imgsz <= 640` で `workers=4`、`imgsz <= 800` で `workers=2`、`imgsz > 800` で `workers=0`）されるように完全自動化。
- **大項目名表記の簡略化**: 設定画面内のグループボックスタイトルから不要な `(...)` 補足表記を削除し、「パス設定」「データ分割」「ハイパーパラメータ」「オーギュメント設定」「推論・自動アノテーション設定」に表記を統一。

---

## [2026-07-29] v2.1.0 - オーギュメント詳細設定追加およびUI改善

### 追加 (Added)
- **ヘルプ画面へのオーギュメント解説追加**: メインヘルプおよび設定ヘルプに「オーギュメント設定 (データ拡張)」の各項目（HSV、回転、平行移動、拡大縮小、シアー、遠近法、反転）の詳細な日本語解説セクションを追加。
- **4.3 ラベル一覧 Excel 自動出力**: `4.3 ラベル一覧` 実行時に `dataset` フォルダ内に `ラベル一覧.xlsx` を自動生成して出力する機能を追加。
- **自動アノテーション実行前のラベルクリアと確認機能**: 自動アノテーション実行時に既存のアノテーションラベル (.txt ファイル) を全削除するか確認するメッセージダイアログを表示し、承認時に自動消去してから実行する機能を追加。
- **オーギュメント詳細設定機能**: HSV（色相・彩度・明度）、回転（degrees）、平行移動（translate）、拡大縮小（scale）、シアー（shear）、遠近法（perspective）、上下/左右反転（flipud, fliplr）をGUIで設定できるように拡張。
- **自動ラベリング結果への識別情報追加**: `autolabel_result.xlsx` にデータ種別 (`train` / `val`) 列を追加。

### 変更 (Changed)
- **UI表記の変更 (`2. ラベル設定` ➔ `2. クラス名設定`)**: メイン画面ボタン、ログメッセージ、ダイアログタイトル、各種ヘルプドキュメントの表記を「クラス名設定」に統一。
- **起動用バッチファイルの整理とログファイル出力の廃止**: 起動スクリプトを `yolo_manager.bat` のみに一本化し、不要なデバッグ用・派生バッチファイルや外部 `.log` ファイルへのリダイレクト出力を全廃。
- **NCNN モデル変換の全自動化とステップ7の統合削除**: 「5. 学習実行」の完了時に自動的に NCNN 形式モデルを併せて生成するよう改善し、手動の「7. NCNNモデル変換」ボタンおよび設定画面の redundant な入力項目を削除・整理（「推論用モデル」に一本化）。
- **用語表記の統一**: ヘルプや関連画面での「オートラベリング」表記を「自動アノテーション」に完全統一。
- **ヘルプ文章の整理と拡充**: メインヘルプから不要なデータ拡張セクションを削除し、設定ダイアログ側のヘルプにオーギュメント各パラメータの詳細な解説（初期値・効果）を追加。
- **設定画面からの『元画像フォルダ』固定入力項目の削除**: 「1. データセット準備」実行時に常にダイアログでコピー元画像フォルダを指定する方式に統一されたため、設定ダイアログおよび設定ファイルからの `元画像フォルダ (source_image_dir)` パス入力項目を削除。
- **Excel ヘッダーの完全日本語化**: `autolabel_result.xlsx` の出力カラム名を全項目日本語（`データ種別`, `画像ファイル名`, `クラス名`, `信頼度`, `検出個数`）に統一。
- **推論ログおよび進捗表示の日本語化**: 推論時などのログ（`Inference` ➔ `推論進捗`、`Saved Excel` ➔ `Excel出力完了`、`Saved Images` ➔ `結果画像保存先`、`Total images` ➔ `処理画像数`）を完全に日本語表示に変更。
- **データセット準備の操作フロー**: 「1. データセット準備」ボタン押下時にコピー元フォルダ選択ダイアログを表示。キャンセルされた場合は `train` / `val` のフォルダ作成のみを行うように変更。
- **labelImg の日本語化**: メニュー、ボタン、ツールチップ、各種メッセージを完全に日本語化。
- **labelImg 保存形式の固定**: 保存フォーマットを YOLO (.txt) に固定化。

### 修正 (Fixed)
- **labelImg 起動・操作時の異常終了 (終了コード 3221226505 / 0xC0000409) の根本解決**:
  - `yolo_io.py` において、`classes.txt` に未定義のクラスインデックスを含むアノテーション `.txt` ファイルを読み込んだ際に発生していた `IndexError` を安全に補正・フォールバックするよう改修。
  - `labelImg.py` の画像読み込み (`read()`) をバイナリ読み込み経由の `QImage.fromData()` に変更し、日本語等のマルチバイト文字列を含む画像パスでのクラッシュを修正。
  - PyQt6 (main.py) と PyQt5 (labelImg.py) の Qt バインディングの競合を避けるため、`LabelImgWorker` による完全独立プロセス起動に対応。
  - `canvas.py` の `wheelEvent`・`paintEvent` (PyQt5 5.15非互換描画ヒントの修正) および `resizeEvent` の再帰呼び出しガードを実装。
- **`main_window.py` 内の `QPlainTextEdit` インポート漏れ修正**: 多行ラベル入力ダイアログで使用されている `QPlainTextEdit` のインポート文を追加し `NameError` を解決。
- **`operations.py` 内の `Union` インポート不全 (NameError) の修正**: `read_text_safe` 追加に伴う型アノテーション `Union` のインポート漏れを修正。
- **バッチファイルのコマンドパース安全化**: Windows の `cmd.exe` でマルチバイト文字が含まれるバッチファイルがパースエラーを起こす問題を、英数標準の安全構文に再構成して完全解消。
- **ラベル設定時のクラッシュ防止とファイル読み込みの安全化**: `classes.txt` 読み込み時に UTF-8 / Shift-JIS (CP932) のマルチエンコーディング対応 (`read_text_safe`) を適用し、例外発生時もアプリが強制終了しないよう try-except で保護。
- **コンソール表示の文字化け修正**: バッチファイル (`yolo_manager.bat` 等) のコードページ設定 (`chcp 65001 > nul`, `set PYTHONUTF8=1`) と Python 標準出力の UTF-8 再設定を組み込み、文字化けを完全修正。
- **NCNN 変換ログの完全クリーン化**: NCNN/PNNX エクスポート実行時に出力されていたすべての内部デバッグ行（`Ultralytics`, `PyTorch:`, `NCNN:`, `summary (fused)`, `Export complete`, `Results saved to`, `inline module`, `pnnx` 等）を自動除外フィルタリングし、ヘッダーと最終結果通知のみを表示するよう簡略化。
- **不要ログの非表示・削除**: スクリプト実行時の `[SCRIPT] 一時ファイル:` 行および `classes.txt 作成:` ログ出力を削除。
- **ログ表示・過去ログ保護処理の根本修整**: 非プログレスキーワードの除外判定と `StartOfBlock` による直前1行選択削除を適用し、推論や学習時などに過去の通常ログ行が一切削除されないように改善。
- **スピンボタン領域の操作性修正**: `QSpinBox` / `QDoubleSpinBox` の矢印ボタン領域の QSS スタイルを定義・拡張し、クリック位置が入力枠フォーカスに吸われないように修正。
- **labelImg 起動ログの抑制**: labelImg 起動時にログエリアへ出力されていた `[CMD]` コマンドラインの非表示化。

---
